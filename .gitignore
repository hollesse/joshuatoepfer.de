_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/
.claude/

# Python — introduced with the .github/scripts/ tooling (infra-004)
__pycache__/
*.py[cod]
*$py.class

# Node — pa11y-ci runs ephemerally via npx (infra-006); no package.json is
# committed, but local runs may leave behind a transient node_modules/.
node_modules/
